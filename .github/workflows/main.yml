name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - name: Build the Docker image
      run: ./selfssl.sh; docker build -t t1 .
      working-directory: ./alpine-nginx-php-fpm-for-transmisson
